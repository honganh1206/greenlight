# Visualizing and analyzing metrics

Things to do:

- Write a script to periodically fetch the JSON data from `GET /debug/vars` for futher analysis
- Use tools like Prometheus to fetch and visualize the data
